# ── Application ─────────────────────────────────────────────────
APP_ID=                    # Identifiant d'application (obligatoire en production)
APP_SECRET=                # Secret d'application (obligatoire en production)

# ── Base de données (Neon / PostgreSQL) ─────────────────────────
DATABASE_URL=              # postgresql://user:pass@host/db?sslmode=require

# ── Stockage des images (Cloudflare R2, API compatible S3) ───────
# Photos produits/galerie (publiques) et captures d'écran D17 (privées,
# servies uniquement à l'admin). Sans ces variables, l'upload est refusé
# avec un message clair mais le reste du site fonctionne.
R2_ACCOUNT_ID=             # Identifiant de compte Cloudflare
R2_ACCESS_KEY_ID=          # Jeton R2 : permission "Object Read & Write" sur le bucket
R2_SECRET_ACCESS_KEY=
R2_BUCKET=                 # Nom exact du bucket (ex. chezlaziz)

# ── Notification e-mail des nouvelles commandes (optionnel) ──────
# Via l'API Resend (https://resend.com). Sans ces variables, aucune
# notification n'est envoyée — la commande est quand même enregistrée.
RESEND_API_KEY=
EMAIL_FROM=                # ex. "Chez Laziz <commandes@chezlaziz.com>" (domaine vérifié chez Resend)
ADMIN_NOTIFY_EMAIL=        # ex. contact@chezlaziz.com

# ── Groupe Telegram : commandes + matbakh (optionnel) ────────────
# Sonne sur le téléphone dès qu'une commande arrive, avec les boutons
# ✅ تأكيد / ❌ إلغاء, et tient à jour le tableau des kilos à préparer.
# Telegram permet de choisir un son propre à cette conversation.
#
# Elles pilotent aussi le rappel des commandes qui dorment (toutes les
# 20 min, 8h-21h heure de Tunis) et les boutons de raison d'annulation.
#
# Ces deux variables suffisent : le webhook est déclaré tout seul au
# démarrage du serveur, et son secret est dérivé du jeton du bot.
# Le bot doit être ADMIN du groupe (pour remplacer et épingler le tableau).
# Sans elles, rien n'est envoyé — la commande est quand même enregistrée.
TELEGRAM_BOT_TOKEN=        # donné par @BotFather, ex. 7123456789:AAH...
TELEGRAM_CHAT_ID=          # groupe : identifiant négatif, ex. -1001234567890

# ── Google Analytics 4 (optionnel, côté navigateur) ──────────────
# Sans identifiant, aucun script Google n'est chargé.
VITE_GA_MEASUREMENT_ID=    # ex. G-XXXXXXXXXX

# ── Meta Pixel + Conversions API (optionnel — publicités Facebook/Instagram) ─
# Pixel ID et token : Meta Events Manager (business.facebook.com → Gestionnaire
# d'événements → votre Pixel → Réglages → Conversions API → Générer un jeton).
# Sans ces variables, aucun script Meta n'est chargé et aucun événement n'est
# envoyé — le reste du site (et les commandes) fonctionne normalement.
VITE_META_PIXEL_ID=              # ex. 1234567890123456 (câblage navigateur, même ID que ci-dessous)
META_PIXEL_ID=                   # même ID que VITE_META_PIXEL_ID (câblage serveur, Conversions API)
META_CONVERSIONS_API_TOKEN=      # jeton d'accès Conversions API du même Pixel
# Code « Test Events » du Gestionnaire d'événements Meta (optionnel) : posé,
# chaque achat envoyé apparaît en direct dans l'onglet de test avec ses
# avertissements. À retirer une fois vérifié — un événement de test ne compte
# pas dans les rapports.
META_TEST_EVENT_CODE=
